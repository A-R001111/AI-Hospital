# =====================================
# backend/requirements.txt
# نسخه جامع و دقیق برای Parspack PaaS
# توضیحات خط به خط با کامنت فارسی
# =====================================

# فریم‌ورک وب سریع و سبک برای API
fastapi==0.102.0  # فریم‌ورک اصلی برای توسعه API

# وب‌سرور ASGI برای اجرای FastAPI
uvicorn[standard]==0.23.2  # شامل uvloop و httptools برای عملکرد بهتر

# درایور PostgreSQL برای Python
psycopg2-binary==2.9.7  # اتصال و کار با پایگاه داده PostgreSQL

# درخواست HTTP به سرورهای دیگر یا APIها
requests==2.31.0  # ارسال درخواست GET/POST و مدیریت پاسخ‌ها

# پردازش فایل‌ها و شناسایی نوع MIME
python-magic==0.4.27  # بررسی نوع فایل‌ها و MIME types

# مدیریت محیط و زمان‌بندی
python-dotenv==1.0.0  # خواندن فایل‌های .env برای تنظیمات محیطی

# ابزارهای کمکی برای مسیرها و فایل‌ها
python-dateutil==2.8.2  # مدیریت تاریخ و زمان پیشرفته

# پردازش JSON و داده‌ها
orjson==3.9.12  # سریع‌ترین کتابخانه JSON برای Python

# ابزار امنیتی
passlib[bcrypt]==1.7.4  # رمزنگاری و مدیریت پسوردها با bcrypt

# مدیریت فایل‌های CSV و Excel
pandas==2.1.1  # پردازش و تحلیل داده‌ها

# پردازش فایل‌های صوتی (در صورت استفاده)
pydub==0.25.1  # کار با فایل‌های صوتی (mp3, wav و غیره)

# پردازش فایل‌های PDF و اسناد
PyPDF2==3.0.1  # خواندن و ویرایش PDF

# ابزارهای کمکی برای عملکرد بهتر و کش
cachetools==5.3.1  # ذخیره‌سازی موقت داده‌ها در حافظه

# اعتبارسنجی داده‌ها
pydantic==2.3.0  # اعتبارسنجی و تعریف مدل داده‌ها

# کتابخانه‌های اضافی (در صورت نیاز پروژه)
python-multipart==0.0.6  # برای دریافت فایل از فرم‌های HTML
email-validator==2.0.0.post1  # اعتبارسنجی ایمیل‌ها
bcrypt==4.1.0  # رمزنگاری امن پسوردها
itsdangerous==2.1.2  # تولید توکن امن برای کارهای رمزنگاری

# =====================================
# نکات مهم:
# 1- فقط پکیج‌های پایتون در این فایل باشند.
# 2- هیچ خط Dockerfile یا RUN داخل این فایل نباشد.
# 3- نسخه‌ها دقیق مشخص شده‌اند تا با Dockerfile سازگار باشند.
# =====================================
